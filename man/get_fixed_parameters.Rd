% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{get_fixed_parameters}
\alias{get_fixed_parameters}
\title{Gets names of parameters declared FIX in the model}
\usage{
get_fixed_parameters(model, kind = NULL)
}
\arguments{
\item{model}{hyperion_nonmem_model object from read_model()}

\item{kind}{character, restrict to "THETA", "OMEGA" or "SIGMA". NULL returns
every kind, ordered theta, omega, sigma.}
}
\value{
character vector of NONMEM parameter names
}
\description{
Fixed status is read from the control stream, so it is available before a run
completes. Omega and sigma are fixed per record, so a fixed BLOCK contributes
its off-diagonal names as well as its diagonals.
}
\examples{
\dontrun{
model <- read_model("model/nonmem/run001.mod")
get_fixed_parameters(model, kind = "OMEGA")
}
}
