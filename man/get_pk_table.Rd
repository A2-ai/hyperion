% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{get_pk_table}
\alias{get_pk_table}
\title{Build the \verb{$PK} block table: one row per statement (internal)}
\usage{
get_pk_table(model)
}
\arguments{
\item{model}{A \code{hyperion_nonmem_model} object from \code{read_model()}}
}
\value{
List of per-statement \code{target}/\code{equation}/\code{symbols} lists.
}
\description{
Returns a list with one element per \verb{$PK} statement. Each element is a list
with \code{target} (assignment LHS, \code{NULL} for non-assignments), \code{equation} (the
statement rendered back to NONMEM source via the pharos AST), and \code{symbols}
(identifiers and \code{THETA}/\code{ETA}/\code{EPS}/\code{ERR} references used, with math
functions excluded). Empty when the model has no \verb{$PK} block.
}
\keyword{internal}
