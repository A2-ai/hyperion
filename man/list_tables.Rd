% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm-tables.R
\name{list_tables}
\alias{list_tables}
\title{List a model's \verb{$TABLE} output records}
\usage{
list_tables(model)
}
\arguments{
\item{model}{A \code{hyperion_nonmem_model} object from \code{\link[=read_model]{read_model()}}.}
}
\value{
A data.frame with one row per \verb{$TABLE} record and columns:
\describe{
\item{index}{position in \code{model$tables}, usable as \code{which}}
\item{file}{the output file name from \verb{FILE=}}
\item{firstonly}{\code{TRUE} for FIRSTONLY records}
}
Empty when the model has no \verb{$TABLE} records.
}
\description{
Flattens the model's parsed \verb{$TABLE} records into a data.frame for
discovery: which output files exist, their index (for \code{\link[=read_nm_table]{read_nm_table()}}),
and whether each is FIRSTONLY (one row per subject) or per-record.
}
\examples{
\dontrun{
mod <- read_model("model/nonmem/run001.mod")
list_tables(mod)
}
}
\seealso{
\code{\link[=read_nm_table]{read_nm_table()}}
}
