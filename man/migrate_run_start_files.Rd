% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{migrate_run_start_files}
\alias{migrate_run_start_files}
\title{Migrate pharos run start files to project-relative model paths}
\usage{
migrate_run_start_files(base_path = NULL)
}
\arguments{
\item{base_path}{Project root the runs were originally recorded under.
Only needed when the recorded absolute paths fall outside the current
project root, for example runs executed from another user's home
directory.}
}
\value{
nothing
}
\description{
Rewrites every \code{pharos_start.json} under the project root, replacing the
absolute \code{model_canonical_path} written by older pharos versions with a
\code{model_path} relative to the project root. Files already in the current
format are left untouched. \code{.git} and \code{rv} directories are skipped.
}
\examples{
\dontrun{
migrate_run_start_files()
migrate_run_start_files(base_path = "/data/user-homes/analyst1/project-root")
}
}
