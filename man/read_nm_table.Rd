% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm-tables.R
\name{read_nm_table}
\alias{read_nm_table}
\title{Read a NONMEM \verb{$TABLE} output file}
\usage{
read_nm_table(model, which = NULL)
}
\arguments{
\item{model}{A \code{hyperion_nonmem_model} object from \code{\link[=read_model]{read_model()}}.}

\item{which}{Which table to read. One of:
\code{NULL} (the only table, error if there are several);
an integer index into \code{\link[=list_tables]{list_tables()}};
a bare file name (resolved against the run output directory); or
a path containing \code{/} (taken as a config-relative path, the manual
override when the output directory cannot be derived from the model).}
}
\value{
A data.frame with every column parsed as double.
}
\description{
Reads one of a model's \verb{$TABLE} output files into a data.frame of doubles.
This is raw I/O only: every column is parsed as numeric (NONMEM writes
undefined values as \code{NaN}/\code{Inf}), with no decoding or metadata applied.
}
\examples{
\dontrun{
mod <- read_model("model/nonmem/run001.mod")
read_nm_table(mod)                 # the only $TABLE
read_nm_table(mod, "patab1")       # by file name
read_nm_table(mod, which = 2)      # by index
}
}
\seealso{
\code{\link[=list_tables]{list_tables()}}
}
