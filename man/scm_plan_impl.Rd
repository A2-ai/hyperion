% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{scm_plan_impl}
\alias{scm_plan_impl}
\title{Build and validate an SCM plan (runs nothing) and write its plan.json}
\usage{
scm_plan_impl(
  config,
  num_rounds = NULL,
  max_retries = NULL,
  cov_step = NULL,
  initial = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{config}{path to the SCM config file (TOML) written by
\code{\link[=scm_init]{scm_init()}}: model, direction, forward_alpha, backward_alpha,
max_retries, cov_step, and the \verb{[covariates]} section (initial, off,
lower, upper, effects). Relative paths resolve against the config file}

\item{num_rounds}{pause after this many rounds per run (NULL = no cap)}

\item{max_retries}{override the config's retries per failed fit}

\item{cov_step}{override whether generated models run the covariance step}

\item{initial}{override the \verb{[covariates]} section's default \code{initial},
where an effect is released the first time it is tested}

\item{overwrite}{replace existing SCM output from a different plan}
}
\value{
a \code{hyperion_scm_plan} object; its \code{plan_path} attribute is the
\code{plan.json} just written, and its \code{context} attribute is where the
SCM process in the out_dir already stands plus what this plan changed about
the plan.json it replaced
}
\description{
Internal engine behind \code{\link[=scm_plan]{scm_plan()}}; use that instead.
}
\keyword{internal}
